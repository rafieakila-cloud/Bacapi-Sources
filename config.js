// Bacapi open-source config — kernel V5.6
// AI engine SENGAJA dilepas di versi open source.
// Mau aktifkan AI? Set AI_ENABLED=true dan isi provider sendiri,
// atau pakai V5.7 (pengembangan privat).
window.BACAPI = {
  KERNEL: '5.6-open',
  AI_ENABLED: false,
  PROVIDER: null // contoh: { url, key, model }
};
